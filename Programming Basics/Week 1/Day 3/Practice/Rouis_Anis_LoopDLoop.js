//when we need to use a confition that repeats itself until it finds the right answer
//the loop should always start at 0 and would only change if requested
//it should stop when it reaches the correct number for ex when it reaches 6 miles
//when the incremented number reaches the condition added
//
var i=0;
for(i=2,i<=6,i++){
    console.log('Candy!')
}